import {
  env,
  port,
  render,
  rootDir,
  templatesDir,
  staticDir,
  publicDir,
  nodeModulesDir,
} from './base';
import routes from './routes';

export default {
  env,
  port,
  render,
  rootDir,
  templatesDir,
  staticDir,
  publicDir,
  nodeModulesDir,
  routes,
};
